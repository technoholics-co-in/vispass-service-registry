APP_ENV=dev

DB_HOST=postgres
DB_PORT=5432
DB_NAME=registry
DB_USER=postgres
DB_PASSWORD=postgres

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PREFIX=service-registry:
SERVICE_CACHE_TTL=300

# Phase 5: set true when behind mesh/ingress with verified client certs
MTLS_REQUIRED=false
